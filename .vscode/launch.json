{
    "version": "0.2.0",
    "configurations": [

        {
        "name": "C++ Launch",
        "type": "cppdbg",
        "request": "launch",
        "program": "${workspaceRoot}/build/ohjelma",
        "preLaunchTask": "make",
        "cwd": "${workspaceRoot}",
        "linux":{
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb"
        }
        }

    ]
}